# @web3viz/playground

> **⚠️ BROKEN BUILD** — This app has a broken import and is excluded from `npm run build`. Do **not** include it in turbo build pipelines.

Interactive playground for testing visualization components in isolation. Next.js app on port 3200.

## Status

- Has a broken import that prevents compilation
- Excluded from root `tsconfig.json`
- Use `npm run dev:playground` to attempt running standalone

## Dependencies

Depends on: `@web3viz/core`, `@web3viz/providers`, `@web3viz/react-graph`, `@web3viz/ui`, `@web3viz/utils`
