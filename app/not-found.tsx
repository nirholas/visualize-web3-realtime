export default function NotFound() {
  return (
    <div
      style={{
        alignItems: 'center',
        display: 'flex',
        fontFamily: "'IBM Plex Mono', monospace",
        height: '100vh',
        justifyContent: 'center',
      }}
    >
      <h1 style={{ fontSize: 14, fontWeight: 400 }}>404 — Page not found</h1>
    </div>
  );
}
